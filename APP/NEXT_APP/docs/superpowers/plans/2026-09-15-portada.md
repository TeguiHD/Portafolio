# Portada viva — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir las secciones de la portada `/` por el diseño validado en el prototipo `taller-portada.html` (mesa giratoria V1, cinta, mazo, coverflow, órbita, centinela, contacto sin tarjetas), con fondo vivo, movimiento por scroll y el cursor original, cumpliendo las reglas de rendimiento y accesibilidad de la especificación, y desplegarlo con cuidado en el VPS compartido.

**Architecture:** Todo lo nuevo vive en `src/modules/landing/portada/` (motor de movimiento GSAP + ScrollTrigger + Lenis, niveles, fondo, hero, secciones, datos, CSS plano bajo `.portada`). `page.tsx` conserva `LandingMotionProvider`, `VisualEnhancements` (DropCursor), `Navbar`, `NoScriptLanding`, `ClosingSignature` y `FooterSection`. `DeferredLandingSection` sigue difiriendo las secciones bajo el pliegue; los stubs con los nombres finales se crean en la tarea 1 para que las tareas 3–8 puedan ejecutarse en paralelo sin tocar archivos compartidos.

**Tech Stack:** Next.js 16.3 App Router (leer `node_modules/next/dist/docs/` antes de tocar código de Next), React 19.2, TypeScript, Tailwind 4 + CSS plano, GSAP 3.12.5 + ScrollTrigger, Lenis 1.3.16, `qrcode` 1.5, `next/font/google`, Playwright 1.57 (solo e2e; no hay runner unitario).

**Spec:** `docs/superpowers/specs/2026-09-15-portada-design.md`

**Referencia de implementación:** el prototipo aprobado `taller-portada.html` (copia en `docs/superpowers/specs/2026-09-15-portada-prototipo.html`, añadida en la tarea 1). Las tareas citan sus bloques por comentario (`/* ───── nombre ───── */`) para portar la lógica; el CSS del prototipo está en su `<style>` bajo los mismos nombres de clase.

## Global Constraints

- Sin scripts en línea salvo el de entrada del hero, renderizado en servidor con `nonce={await getNonce()}` (`@/lib/nonce`). Sin `onclick` en atributos. Sin orígenes nuevos en la CSP.
- Textos exactos de la especificación; ningún texto de "IA", ninguna nota explicativa, ninguna cifra comercial (`200%`, `92%`, `850+`, `Auditado`) en `#casos`.
- Anclajes obligatorios: `#hero`, `#tools-belt`, `#vault`, `#casos` (con `h2#projects-title`), `#tecnologias`, `#architecture`, `#contact`, `#closing-signature`, `#site-footer`. Enlace "Usar herramientas" → `/herramientas`.
- Un solo `h1` en `/`; ningún `h1` dentro de `<noscript>`.
- Nivel de movimiento: `estatico` si `useLandingMotion().allowed` es falso; `medio` si `hardwareConcurrency ≤ 4`, `deviceMemory ≤ 4`, ancho `< 768` o `(pointer: coarse)`; `completo` en el resto. Toda animación en bucle debe pararse en `estatico` y cuando su sección no está en el viewport.
- El párrafo del hero (elemento LCP) nunca se anima ni se oculta. Ningún elemento del hero queda oculto más de 700 ms.
- Puerta de cada commit: `pnpm typecheck && pnpm lint && pnpm seo:audit` en verde. Pruebas: servidor de desarrollo en `http://localhost:3000` (`pnpm dev` en segundo plano) y `REDIS_URL='' npx playwright test tests/e2e/<spec> --workers=1 --reporter=line`.
- Ediciones de TSX con Python o con la herramienta de edición, nunca con `perl -pi` y `|` en el patrón.
- No confirmar nunca los archivos con trabajo de seguridad en curso: `.env.example`, `README.md`, `src/app/admin/cotizaciones/*`, `src/app/admin/gestion-comercial/*`, `src/lib/api-security.ts`, `src/lib/enforcement-executor.ts`, `src/lib/security-logger.ts`, `src/modules/admin/clients/actions.ts`, `src/proxy.ts`, `src/services/quotation-access.ts`, `DOCKER/Dockerfile.web`, `README-MEJORAS-SEGURIDAD-VPS.md`, `deploy-unblock.sh`. Antes de cada commit: `git checkout -- next-env.d.ts`.
- Commits en español, con `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

---

## Estructura de archivos

```
src/modules/landing/portada/
  nivel.ts                      usePortadaNivel(): Nivel; presupuesto del dispositivo
  PortadaMotion.tsx             proveedor cliente: GSAP+ScrollTrigger, Lenis, autoscroll central, barra de progreso; usePortada()
  revelar.ts                    revelarCabecera(el, gsap) y useRevelar(ref)
  interaccion.ts                useMagnetico(ref), onda(e) (onda al pulsar), useInclinar(ref, max)
  FondoVivo.tsx                 lienzo fijo de partículas (sustituye a BackgroundManager)
  EntradaHero.tsx               <script nonce> de retención de 700 ms (servidor)
  portada.css                   tokens, cabeceras, CTA, chips, reveal, barra de progreso, utilidades .portada
  hero/HeroSection.tsx          servidor: texto LCP + <MesaGiratoria/>
  hero/MesaGiratoria.tsx        cliente: giro V1, puntos, avance automático, entrada
  hero/hero.css
  hero/instrumentos/Instrumento.tsx        marco común (studio-panel + mesa)
  hero/instrumentos/QuitarFondo.tsx        demo 0
  hero/instrumentos/GeneradorQR.tsx        demo 1
  hero/instrumentos/Paleta.tsx             demo 2
  hero/instrumentos/Recortar.tsx           demo 3
  hero/instrumentos/tipos.ts               interface Demo { start(): void; stop(): void }
  secciones/Mazo.tsx + mazo.css            #vault
  secciones/Coverflow.tsx + coverflow.css  #casos (servidor + isla cliente CoverflowEscena.tsx)
  secciones/Orbita.tsx + orbita.css        #tecnologias
  secciones/Centinela.tsx + centinela.css  #architecture (+ matrixOrb.ts)
  datos/instrumentos.ts, mazo.ts, casos.ts, tecnologias.ts, seguridad.ts
src/modules/landing/sections/vault-demos.tsx   FinanceDemo, CVOptimizerDemo, SecurityDemo extraídos
src/app/page.tsx                                orden final
src/app/layout.tsx                              next/font (Inter, JetBrains Mono)
src/modules/landing/layout/VisualEnhancements.tsx  ya no monta BackgroundManager
src/modules/landing/sections/DeferredLandingSection.tsx  registra Mazo, Orbita, Centinela
src/modules/landing/sections/ToolsBeltSection.tsx        texto + revelado + inclinación
src/modules/landing/sections/ContactSection.tsx          sin tarjetas
docs/licencias/rareui-matrix-orb.md
tests/e2e/portada-*.spec.ts (uno por tarea) y tests/e2e/landing-motion.spec.ts (reescrito)
```

Eliminados al final (tarea 9): `HeroDashboard.tsx`, `FloatingDashboard.tsx`, `ForbiddenVaultSection.tsx` (tras extraer sus demos), `SecurityArchitectureSection.tsx`, `TechnologiesSection.tsx`, `BackgroundManager.tsx`.

---

### Task 1: Base común — fuentes, niveles, motor de movimiento, fondo vivo, esqueleto y stubs

**Files:**
- Modify: `src/app/layout.tsx` (fuentes en `<html className>`), `src/app/globals.css:68-70`
- Create: `src/modules/landing/portada/nivel.ts`, `PortadaMotion.tsx`, `revelar.ts`, `interaccion.ts`, `FondoVivo.tsx`, `EntradaHero.tsx`, `portada.css`
- Create stubs: `secciones/Mazo.tsx`, `secciones/Orbita.tsx`, `secciones/Centinela.tsx`, `hero/HeroSection.tsx`
- Modify: `src/modules/landing/sections/DeferredLandingSection.tsx:21-49`, `src/modules/landing/layout/VisualEnhancements.tsx`, `src/app/page.tsx`
- Copy: prototipo a `docs/superpowers/specs/2026-09-15-portada-prototipo.html`
- Test: `tests/e2e/portada-base.spec.ts`

**Interfaces:**
- Produces: `usePortadaNivel(): Nivel` y `type Nivel = "completo" | "medio" | "estatico"` (`nivel.ts`); `usePortada(): { nivel: Nivel; lenis: Lenis | null; listo: boolean }` (`PortadaMotion.tsx`); `useRevelar<T extends HTMLElement>(): RefObject<T>` que revela la cabecera marcada con `data-revelar` (eyebrow, `.ln > span`, `p`) al entrar; `useMagnetico(ref, fuerza = 8)`, `onda(e: React.PointerEvent)`, `useInclinar(ref, max = 8)` (`interaccion.ts`); clase CSS `.portada` con tokens `--p-bg #0a0a0f`, `--p-panel #111923`, `--p-linea #ffffff14`, `--p-linea2 #ffffff26`, `--p-txt #e6edf3`, `--p-mut #94a3b8`, `--p-teal #5eead4`, `--p-ambar #f59e0b`, `--p-vio #a78bfa`, `--p-ease cubic-bezier(.2,.8,.2,1)`; clases `.p-wrap` (max 1180 px), `.p-cab` (cabecera: `.eyebrow`, `h2 .ln > span`, `.sub`), `.p-cta` (con `.fantasma`, `.ambar`), `.p-chip`, `.p-sec` (padding-block clamp(72px, 10vw, 128px)), `.div-luz`.
- Consumes: `useLandingMotion()` de `@/modules/landing/motion/LandingMotionProvider`; `getNonce` de `@/lib/nonce`.

- [ ] **Step 1: Fuentes con next/font**

En `src/app/layout.tsx`, tras los imports:

```tsx
import { Inter, JetBrains_Mono } from "next/font/google";

const inter = Inter({ subsets: ["latin"], display: "swap", variable: "--font-inter", weight: "variable" });
const mono = JetBrains_Mono({ subsets: ["latin"], display: "swap", variable: "--font-mono", weight: ["500", "700"] });
```

y en el `<html>`: `<html lang="es" suppressHydrationWarning className={`${inter.variable} ${mono.variable}`}>`. En `globals.css` líneas 69–70 las variables `--font-inter` y `--font-mono` del `:root` se eliminan (las define `next/font` en `<html>`); se añade `--font-inter-respaldo: ui-sans-serif, system-ui, sans-serif;` y en `html, body` `font-family: var(--font-inter, var(--font-inter-respaldo));`. Comprobar con `pnpm typecheck` y en el navegador que `getComputedStyle(document.body).fontFamily` empieza por `Inter`.

- [ ] **Step 2: nivel.ts**

```ts
"use client";
import { useEffect, useState } from "react";
import { useLandingMotion } from "@/modules/landing/motion/LandingMotionProvider";

export type Nivel = "completo" | "medio" | "estatico";

type NavegadorConPresupuesto = Navigator & { deviceMemory?: number; connection?: { saveData?: boolean } };

export function presupuestoBajo(): boolean {
  if (typeof window === "undefined") return true;
  const n = navigator as NavegadorConPresupuesto;
  return (
    (typeof n.hardwareConcurrency === "number" && n.hardwareConcurrency <= 4) ||
    (typeof n.deviceMemory === "number" && n.deviceMemory <= 4) ||
    Boolean(n.connection?.saveData) ||
    window.innerWidth < 768 ||
    window.matchMedia("(pointer: coarse)").matches
  );
}

/** Nivel de movimiento de la portada. Empieza en "estatico" hasta montar (SSR estable). */
export function usePortadaNivel(): Nivel {
  const { allowed } = useLandingMotion();
  const [bajo, setBajo] = useState(true);
  useEffect(() => {
    const medir = () => setBajo(presupuestoBajo());
    medir();
    window.addEventListener("resize", medir, { passive: true });
    return () => window.removeEventListener("resize", medir);
  }, []);
  if (!allowed) return "estatico";
  return bajo ? "medio" : "completo";
}
```

- [ ] **Step 3: PortadaMotion.tsx**

```tsx
"use client";
import { createContext, useContext, useEffect, useRef, useState, type ReactNode } from "react";
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import Lenis from "lenis";
import { usePortadaNivel, type Nivel } from "./nivel";

gsap.registerPlugin(ScrollTrigger);

interface Portada { nivel: Nivel; lenis: Lenis | null; listo: boolean }
const Ctx = createContext<Portada>({ nivel: "estatico", lenis: null, listo: false });
export function usePortada() { return useContext(Ctx); }

export function PortadaMotion({ children }: { children: ReactNode }) {
  const nivel = usePortadaNivel();
  const [lenis, setLenis] = useState<Lenis | null>(null);
  const [listo, setListo] = useState(false);
  const progreso = useRef<HTMLDivElement>(null);

  // Lenis solo en "completo"; ScrollTrigger sigue al scroll nativo en los demás niveles.
  useEffect(() => {
    if (nivel !== "completo") { setLenis(null); return; }
    const l = new Lenis({ lerp: 0.11, smoothWheel: true });
    l.on("scroll", ScrollTrigger.update);
    const tick = (t: number) => l.raf(t * 1000);
    gsap.ticker.add(tick);
    gsap.ticker.lagSmoothing(0);
    setLenis(l);
    // Lenis virtualiza la rueda: el autoscroll del botón central se reproduce a mano.
    let ancla: number | null = null, v = 0;
    const abajo = (e: PointerEvent) => { if (e.button !== 1) return; e.preventDefault(); ancla = e.clientY; v = 0; };
    const mueve = (e: PointerEvent) => { if (ancla === null) return; const d = e.clientY - ancla; v = Math.abs(d) < 8 ? 0 : Math.max(-38, Math.min(38, (d - Math.sign(d) * 8) * 0.09)); };
    const suelta = () => { ancla = null; v = 0; };
    const auto = (_t: number, dt: number) => { if (ancla === null || !v) return; l.scrollTo(l.scroll + v * (dt / 16.67), { immediate: true }); };
    window.addEventListener("pointerdown", abajo, { passive: false });
    window.addEventListener("pointermove", mueve, { passive: true });
    window.addEventListener("pointerup", suelta); window.addEventListener("pointercancel", suelta); window.addEventListener("blur", suelta);
    gsap.ticker.add(auto);
    return () => {
      gsap.ticker.remove(tick); gsap.ticker.remove(auto);
      window.removeEventListener("pointerdown", abajo); window.removeEventListener("pointermove", mueve);
      window.removeEventListener("pointerup", suelta); window.removeEventListener("pointercancel", suelta); window.removeEventListener("blur", suelta);
      l.destroy(); setLenis(null);
    };
  }, [nivel]);

  // Barra de progreso de lectura y estado de ScrollTrigger según nivel.
  useEffect(() => {
    if (nivel === "estatico") { ScrollTrigger.getAll().forEach((st) => st.disable(false)); return; }
    ScrollTrigger.getAll().forEach((st) => st.enable());
    const tw = gsap.to(progreso.current, { scaleX: 1, ease: "none", scrollTrigger: { trigger: document.body, start: "top top", end: "bottom bottom", scrub: 0.3 } });
    setListo(true);
    return () => { tw.scrollTrigger?.kill(); tw.kill(); };
  }, [nivel]);

  return (
    <Ctx.Provider value={{ nivel, lenis, listo }}>
      <div ref={progreso} className="p-progreso" aria-hidden="true" data-nivel={nivel} />
      {children}
    </Ctx.Provider>
  );
}
```

`data-nivel` en la barra sirve a las pruebas para leer el nivel efectivo. En `page.tsx`, `PortadaMotion` envuelve `<main>` dentro de `LandingMotionProvider`.

- [ ] **Step 4: revelar.ts e interaccion.ts**

```ts
"use client";
import { useEffect, useRef, type RefObject } from "react";
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { usePortada } from "./PortadaMotion";

/** Revela una cabecera: eyebrow sube, líneas del título (.ln > span) entran por máscara, párrafo sube. */
export function revelarCabecera(cab: HTMLElement): gsap.core.Timeline {
  const tl = gsap.timeline({ scrollTrigger: { trigger: cab, start: "top 82%", once: true }, defaults: { ease: "power3.out" } });
  const eyebrow = cab.querySelectorAll(".eyebrow"), lineas = cab.querySelectorAll(".ln > span"), sub = cab.querySelectorAll(".sub");
  if (eyebrow.length) tl.from(eyebrow, { y: 12, opacity: 0, duration: 0.5 });
  if (lineas.length) tl.from(lineas, { yPercent: 110, duration: 0.9, stagger: 0.1 }, "-=.3");
  if (sub.length) tl.from(sub, { y: 14, opacity: 0, duration: 0.6 }, "-=.5");
  return tl;
}

export function useRevelar<T extends HTMLElement = HTMLDivElement>(): RefObject<T | null> {
  const ref = useRef<T>(null);
  const { nivel, listo } = usePortada();
  useEffect(() => {
    const el = ref.current;
    if (!el || !listo || nivel === "estatico") return;
    const tl = revelarCabecera(el);
    return () => { tl.scrollTrigger?.kill(); tl.kill(); gsap.set(el.querySelectorAll(".eyebrow, .ln > span, .sub"), { clearProps: "all" }); };
  }, [nivel, listo]);
  return ref;
}
```

```ts
"use client";
import { useEffect, type PointerEvent as ReactPointerEvent, type RefObject } from "react";
import { gsap } from "gsap";
import { usePortada } from "./PortadaMotion";

/** Onda al pulsar (solo botón principal). Requiere `position: relative; overflow: hidden` en el elemento. */
export function onda(e: ReactPointerEvent<HTMLElement>) {
  if (e.button !== 0) return;
  const b = e.currentTarget, r = b.getBoundingClientRect(), o = document.createElement("span");
  o.className = "p-onda"; const s = Math.max(r.width, r.height) * 2;
  o.style.cssText = `width:${s}px;height:${s}px;left:${e.clientX - r.left}px;top:${e.clientY - r.top}px`;
  b.appendChild(o); o.addEventListener("animationend", () => o.remove());
}

/** Imán de 8 px con puntero fino en nivel completo. */
export function useMagnetico(ref: RefObject<HTMLElement | null>, fuerza = 8) {
  const { nivel } = usePortada();
  useEffect(() => {
    const el = ref.current;
    if (!el || nivel !== "completo" || !matchMedia("(hover: hover) and (pointer: fine)").matches) return;
    const qx = gsap.quickTo(el, "x", { duration: 0.4, ease: "power3.out" }), qy = gsap.quickTo(el, "y", { duration: 0.4, ease: "power3.out" });
    const mover = (e: PointerEvent) => { const r = el.getBoundingClientRect(); qx(((e.clientX - r.left) / r.width - 0.5) * fuerza * 2); qy(((e.clientY - r.top) / r.height - 0.5) * fuerza * 2); };
    const salir = () => { qx(0); qy(0); };
    el.addEventListener("pointermove", mover); el.addEventListener("pointerleave", salir);
    return () => { el.removeEventListener("pointermove", mover); el.removeEventListener("pointerleave", salir); gsap.set(el, { clearProps: "transform" }); };
  }, [ref, nivel, fuerza]);
}

/** Inclinación 3D con brillo (`--gx`/`--gy` para .p-brillo). */
export function useInclinar(ref: RefObject<HTMLElement | null>, max = 8) {
  const { nivel } = usePortada();
  useEffect(() => {
    const el = ref.current;
    if (!el || nivel !== "completo" || !matchMedia("(hover: hover) and (pointer: fine)").matches) return;
    const rx = gsap.quickTo(el, "rotateX", { duration: 0.5, ease: "power3.out" }), ry = gsap.quickTo(el, "rotateY", { duration: 0.5, ease: "power3.out" });
    const mover = (e: PointerEvent) => { const r = el.getBoundingClientRect(), px = (e.clientX - r.left) / r.width, py = (e.clientY - r.top) / r.height; ry((px - 0.5) * max * 2); rx((0.5 - py) * max * 2); el.style.setProperty("--gx", `${px * 100}%`); el.style.setProperty("--gy", `${py * 100}%`); };
    const salir = () => { rx(0); ry(0); };
    gsap.set(el, { transformPerspective: 900 });
    el.addEventListener("pointermove", mover); el.addEventListener("pointerleave", salir);
    return () => { el.removeEventListener("pointermove", mover); el.removeEventListener("pointerleave", salir); gsap.set(el, { clearProps: "transform" }); };
  }, [ref, nivel, max]);
}
```

- [ ] **Step 5: FondoVivo.tsx**

Portar el bloque `/* ───── fondo vivo ───── */` del prototipo (desde `var canvas = document.getElementById("fondo")` hasta el cierre de su `gsap.ticker.add`) a un componente cliente:

```tsx
"use client";
import { useEffect, useRef } from "react";
import { gsap } from "gsap";
import { usePortada } from "./PortadaMotion";

const TINTES: Record<string, string> = { hero: "#5eead4", "tools-belt": "#8bd5c3", vault: "#f59e0b", casos: "#f5b04a", tecnologias: "#a78bfa", architecture: "#fb7185", contact: "#f59e0b" };

export function FondoVivo() {
  const { nivel, lenis } = usePortada();
  const ref = useRef<HTMLCanvasElement>(null);
  useEffect(() => {
    const canvas = ref.current;
    if (!canvas || nivel === "estatico") return;
    // … cuerpo del prototipo con estas adaptaciones:
    // - presupuesto = nivel === "medio" ? 0.6 : 1; dpr = presupuesto < 1 ? 1 : Math.min(devicePixelRatio || 1, 1.5)
    // - el tinte por sección se toma con un IntersectionObserver (rootMargin "-45% 0px -45% 0px") sobre los ids de TINTES, no con ScrollTrigger
    // - la velocidad de scroll sale de lenis?.on("scroll") si hay Lenis, o del evento scroll nativo
    // - la aurora se pinta en un lienzo de 1/8 y se escala con drawImage (como el prototipo v13)
    // - 30 fps efectivos cuando presupuesto < 1 (acumulador de dt); calidad adaptativa (media de frame > 25 ms → -0.2, mínimo 0.5)
    // - el ticker se retira en la limpieza y el lienzo se vacía (canvas.width = 1)
    return () => { /* quitar listeners, observer y ticker; canvas.width = 1 */ };
  }, [nivel, lenis]);
  if (nivel === "estatico") return null;
  return <canvas ref={ref} className="p-fondo" data-landing-background="true" aria-hidden="true" />;
}
```

CSS en `portada.css`: `.p-fondo { position: fixed; inset: 0; width: 100%; height: 100%; z-index: 0; pointer-events: none; }` y `.portada > * { position: relative; z-index: 1; }` para que las secciones queden encima. El cuerpo del efecto se transcribe literal del prototipo (partículas en tres capas `[.32,.62,1]`, atracción del ratón < 200 px, ondas de clic con botón 0, paralaje `(.04 + prof*.14)` por scroll, estiramiento por velocidad, lotes Path2D por capa, uniones con rejilla espacial `LIM 135` y tope 420 trazos, dos auroras).

- [ ] **Step 6: EntradaHero.tsx (servidor)**

```tsx
import { getNonce } from "@/lib/nonce";

/** Retiene título y mesa del hero como mucho 700 ms para que la coreografía no destelle; el párrafo (LCP) nunca se retiene. */
export async function EntradaHero() {
  const nonce = await getNonce();
  const codigo = `(function(){var h=document.documentElement;if(matchMedia("(prefers-reduced-motion: reduce)").matches||(navigator.connection&&navigator.connection.saveData))return;h.classList.add("portada-entrada");setTimeout(function(){h.classList.remove("portada-entrada")},700)})();`;
  return <script nonce={nonce} dangerouslySetInnerHTML={{ __html: codigo }} />;
}
```

CSS: `html.portada-entrada .p-hero h1 .ln > span, html.portada-entrada .p-hero .p-mesa, html.portada-entrada .p-hero .p-cta, html.portada-entrada .p-hero .p-insignia { visibility: hidden; }`.

- [ ] **Step 7: portada.css**

Transcribir del `<style>` del prototipo, bajo `.portada`: variables, `.wrap` → `.p-wrap`, `.sec` → `.p-sec`, `.cab` → `.p-cab` (con `.eyebrow`, `h2`, `.ln`, `.sub`), `.cta` → `.p-cta` (variantes `.fantasma`, `.ambar`, `.p-onda` y `@keyframes p-onda`), `.chip` → `.p-chip`, `.div-luz`, `.glare` → `.p-brillo`, `#progreso` → `.p-progreso`, y `@media (prefers-reduced-motion: reduce) .portada * { animation: none !important; transition: none !important; }`. Sin `backdrop-filter` fuera de la barra de navegación.

- [ ] **Step 8: Stubs, registro y página**

Crear `secciones/Mazo.tsx`, `secciones/Orbita.tsx`, `secciones/Centinela.tsx` como componentes cliente que devuelven `<section id="vault|tecnologias|architecture" className="p-sec" />` con la cabecera real (eyebrow/h2/sub de la especificación) y nada más; `hero/HeroSection.tsx` que renderiza el texto actual de `HeroContent` (mismo markup, sin `HeroDashboard`) más `<EntradaHero />`. En `DeferredLandingSection.tsx` cambiar los `load` de `vault`, `tecnologias` y `architecture` a `(await import("../portada/secciones/Mazo")).Mazo`, `…Orbita`, `…Centinela`. En `VisualEnhancements.tsx` eliminar `BackgroundManager` (import y render). En `page.tsx`:

```tsx
import "@/modules/landing/portada/portada.css";
import { PortadaMotion } from "@/modules/landing/portada/PortadaMotion";
import { FondoVivo } from "@/modules/landing/portada/FondoVivo";
import { HeroSection } from "@/modules/landing/portada/hero/HeroSection";
// ShowcaseSection se sustituye en la tarea 5 por Coverflow; hasta entonces sigue igual.
…
<LandingMotionProvider>
  <PortadaMotion>
    <FondoVivo />
    <VisualEnhancements />
    <main className="portada relative min-h-screen selection:bg-accent-success/30">…</main>
  </PortadaMotion>
  <ClosingSignature />
  <FooterSection />
</LandingMotionProvider>
```

Copiar el prototipo: `cp <taller>/taller-portada.html docs/superpowers/specs/2026-09-15-portada-prototipo.html`.

- [ ] **Step 9: Prueba `tests/e2e/portada-base.spec.ts`**

```ts
import { test, expect } from "@playwright/test";

test("la base de la portada: fondo vivo, barra de progreso, tipografía y sin desbordamientos", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await expect(page.getByRole("link", { name: "Usar herramientas", exact: true })).toBeVisible();
  await expect(page.locator("[data-landing-background]")).toHaveCount(1);
  await expect(page.locator(".p-progreso")).toHaveAttribute("data-nivel", "completo");
  expect(await page.evaluate(() => getComputedStyle(document.body).fontFamily)).toMatch(/Inter/);
  const parrafo = page.locator("#hero p").first();
  await expect(parrafo).toHaveCSS("visibility", "visible");
  for (const width of [320, 375, 768, 1024, 1440]) {
    await page.setViewportSize({ width, height: 900 });
    expect(await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth)).toBe(true);
  }
});

test("movimiento reducido: nivel estático, sin fondo", async ({ browser }) => {
  const ctx = await browser.newContext({ reducedMotion: "reduce", viewport: { width: 1440, height: 1000 } });
  const page = await ctx.newPage();
  await page.goto("/");
  await expect(page.locator(".p-progreso")).toHaveAttribute("data-nivel", "estatico");
  await expect(page.locator("[data-landing-background]")).toHaveCount(0);
  await ctx.close();
});

test("pausar efectos apaga el fondo y persiste", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await page.getByRole("button", { name: "Pausar efectos" }).click();
  await expect(page.locator("[data-landing-background]")).toHaveCount(0);
  await page.reload();
  await expect(page.getByRole("button", { name: "Activar efectos" })).toBeVisible();
});
```

- [ ] **Step 10: Ejecutar, puerta y commit**

`REDIS_URL='' npx playwright test tests/e2e/portada-base.spec.ts --workers=1 --reporter=line` → 3 verdes. `pnpm typecheck && pnpm lint && pnpm seo:audit`. Commit: `feat(portada): base común — fuentes, niveles, motor de movimiento y fondo vivo`.

---

### Task 2: Hero — mesa giratoria V1 con cuatro instrumentos

**Files:**
- Create: `hero/MesaGiratoria.tsx`, `hero/hero.css`, `hero/instrumentos/tipos.ts`, `Instrumento.tsx`, `QuitarFondo.tsx`, `GeneradorQR.tsx`, `Paleta.tsx`, `Recortar.tsx`, `datos/instrumentos.ts`
- Modify: `hero/HeroSection.tsx` (stub de la tarea 1)
- Test: `tests/e2e/portada-hero.spec.ts`

**Interfaces:**
- Consumes: `usePortada()`, `useMagnetico`, `onda`, componentes de `@/components/tools/mesa/*` (`MesaCabecera`, `MesaEscenario`, `MesaRail`, `MesaBoton`, `MesaGrupo`, `MesaSeparador`, `MesaPasos`, iconos), `qrcode` (`QRCode.create(texto, { errorCorrectionLevel: "M" }).modules`).
- Produces: `interface Demo { start(): void; stop(): void }` (`tipos.ts`); cada instrumento es `forwardRef`-menos: `function QuitarFondo({ registrar }: { registrar: (d: Demo | null) => void })` y equivalentes; `MesaGiratoria` expone `data-instrumento="<id>"` en cada tarjeta, `inert` + `aria-hidden` en las que no están al frente, `data-frente="true"` en la del frente, y puntos `role="tablist"` con botones `role="tab"` (`aria-label` = nombre, `aria-selected`).
- Datos (`datos/instrumentos.ts`): `[{ id: "quitar-fondo", nombre: "Quitar fondo", acento: "#5eead4" }, { id: "qr", nombre: "Generador de QR", acento: "#a78bfa" }, { id: "paleta", nombre: "Extractor de paleta", acento: "#f59e0b" }, { id: "recortar", nombre: "Recortar imagen", acento: "#60a5fa" }]`.

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";

test("la mesa giratoria muestra un instrumento al frente y avanza", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await expect(page.locator("[data-instrumento]")).toHaveCount(4);
  await expect(page.locator("[data-instrumento]:not([inert])")).toHaveCount(1);
  await expect(page.locator('[data-instrumento][data-frente="true"]')).toHaveAttribute("data-instrumento", "quitar-fondo");
  await page.getByRole("tab", { name: "Extractor de paleta" }).click();
  await expect(page.locator('[data-instrumento][data-frente="true"]')).toHaveAttribute("data-instrumento", "paleta");
  await expect(page.locator("[data-instrumento]:not([inert])")).toHaveCount(1);
  // el párrafo LCP nunca se retiene
  await expect(page.locator("#hero p").first()).toHaveCSS("visibility", "visible");
  await expect(page.locator("#hero p").first()).toHaveCSS("opacity", "1");
});

test("las demostraciones paran cuando el hero sale de pantalla y al pausar", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await expect(page.locator('[data-instrumento][data-frente="true"]')).toHaveAttribute("data-motion-active", "true");
  await page.evaluate(() => window.scrollTo(0, 2600)); await page.waitForTimeout(600);
  await expect(page.locator('[data-instrumento][data-motion-active="true"]')).toHaveCount(0);
  await page.evaluate(() => window.scrollTo(0, 0)); await page.waitForTimeout(600);
  await expect(page.locator('[data-instrumento][data-frente="true"]')).toHaveAttribute("data-motion-active", "true");
  await page.getByRole("button", { name: "Pausar efectos" }).click();
  await expect(page.locator('[data-instrumento][data-motion-active="true"]')).toHaveCount(0);
});

test("a 390 px la mesa cabe sin desbordar", async ({ page }) => {
  await page.setViewportSize({ width: 390, height: 844 });
  await page.goto("/");
  await expect(page.locator("[data-instrumento]")).toHaveCount(4);
  expect(await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth)).toBe(true);
});
```

- [ ] **Step 2: Instrumento.tsx (marco común)**

Marco `article.p-instrumento` → `div.studio-panel` (clases de `src/app/herramientas/tools.css`, importado en `hero.css` con `@import "../../../../app/herramientas/tools.css";` y `@import "../../../../components/tools/mesa/mesa.css";`) con `MesaCabecera` + cuerpo `.mesa-cuerpo` (escenario + carril) + `MesaPasos`. Props: `{ id, nombre, acento, cabecera: { nombre, detalle, acciones }, pasos, estados, children (escenario), carril }`. El acento se pasa como `style={{ "--mesa-acento": acento }}`.

- [ ] **Step 3: Las cuatro demostraciones**

Portar del prototipo los cuatro bloques `instrumentosDemo[0..3]` (dentro de `/* arranque y relevo de las demostraciones */` y las IIFEs previas: quitar fondo con `fantasma` y pincel sobre una máscara, QR tecleando `https://nicoholas.dev` y dibujando módulos, paleta extrayendo seis colores de una imagen dibujada, recorte ajustando el marco a proporciones). Cada componente crea sus lienzos con `useRef`, construye su timeline GSAP en `start()` (`if (tl) tl.kill()`), la mata en `stop()` y llama a `registrar({ start, stop })` en un `useEffect` (y `registrar(null)` al desmontar). `tomarControl()` al interactuar: mata la timeline y deja la tarjeta en modo manual hasta el siguiente `start()`. QR: `QRCode.create(texto, { errorCorrectionLevel: "M" }).modules` (`size` y `get(r, c)`), pintado en lienzo. Cada tarjeta lleva la pista de la mesa ("Pinta para borrar restos", "Escribe un texto o enlace", "Pulsa un color para ver sus valores", "Arrastra el marco").

- [ ] **Step 4: MesaGiratoria.tsx**

Portar `colocar(animar)`, `ir(i, manual)`, `radio()` (220 / 150), el giro por scroll (`ScrollTrigger` sobre `#hero`, `scrub .6`, solo `completo`), la inclinación de la mesa (`rotateX: -6` base), la pausa al pasar el ratón (`pausaHasta`), el avance cada 11 s con `heroVisible` (IntersectionObserver) y `document.hidden`, y el relevo de demos (`ir` para las demás; `start` solo la del frente). Las tarjetas no frontales: `inert`, `aria-hidden="true"`, `pointer-events: none`. Atributo `data-motion-active` en cada tarjeta = `"true"` solo si es la del frente, el hero es visible y `nivel !== "estatico"`. En `estatico`: `colocar(false)` y sin demos.

- [ ] **Step 5: Entrada y HeroSection**

En `MesaGiratoria` (o un `useEffect` en un `HeroEntrada` cliente que reciba refs): si `html.classList.contains("portada-entrada")` y nivel ≠ estático, crear la timeline del prototipo (`/* ───── coreografía de entrada del hero ───── */`) sin el párrafo, con `onStart` que quita la clase; si la clase ya no está, no animar. `HeroSection.tsx` (servidor) mantiene el markup de `HeroContent` con las clases nuevas (`.p-hero`, `.p-insignia`, `h1 > span.ln > span.l1/.l2`, `.sub`, `.p-cta`), `data-hero-content`, el ancla "Explorar" a `#tools-belt`, y monta `<MesaGiratoria />` a la derecha (columna 46/54 desde 1024 px; debajo del texto por debajo).

- [ ] **Step 6: Ejecutar, puerta, commit**

Prueba verde; `pnpm typecheck && pnpm lint && pnpm seo:audit`; commit `feat(portada): hero con mesa giratoria y cuatro instrumentos reales`.

---

### Task 3: Cinta de herramientas — texto, revelado e inclinación

**Files:**
- Modify: `src/modules/landing/sections/ToolsBeltSection.tsx:55-70` (cabecera y CTA), `ToolsBeltSection.module.css` (hover)
- Test: `tests/e2e/portada-cinta.spec.ts`

**Interfaces:** Consumes `useRevelar`, `useInclinar`, `useMagnetico`, `onda`, `TOOL_COUNT`.

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("la cinta invita a explorar las herramientas con el texto real", async ({ page }) => {
  await page.goto("/");
  await page.locator("#tools-belt").scrollIntoViewIfNeeded();
  const cinta = page.locator("#tools-belt");
  await expect(cinta).not.toHaveAttribute("aria-busy", "true", { timeout: 10_000 });
  await expect(cinta.getByText("29 herramientas · sin registro")).toBeVisible();
  await expect(cinta.getByRole("heading", { level: 2 })).toContainText("Las herramientas que uso a diario.");
  await expect(cinta.getByRole("heading", { level: 2 })).toContainText("Úsalas tú también.");
  await expect(cinta.getByRole("link", { name: /Explorar herramientas/ })).toHaveAttribute("href", "/herramientas");
  await expect(cinta).not.toContainText("Menos pasos.");
  await expect(cinta.getByRole("heading", { level: 3, name: "Quitar fondo" })).toBeVisible();
});
```

- [ ] **Step 2: Cambios**

Eyebrow: `{TOOL_COUNT} herramientas · sin registro`. `h2 id="tools-heading"`: `<span className="ln"><span>Las herramientas que uso a diario.</span></span><span className="ln"><span>Úsalas tú también.</span></span>`. Párrafo: "Quita el fondo de una foto, recorta, genera un QR o limpia un JSON: rápido, en tu navegador y sin cuenta." CTA: "Explorar herramientas" (`Link` a `/herramientas`, `data-magnetic`, `onPointerDown={onda}`, clase `p-cta`). Cabecera con `ref={useRevelar()}` y clases `.eyebrow`, `.sub`. Tarjetas grandes (`.card`) con `useInclinar(ref, 6)` y `.p-brillo`. Mantener `TOOL_COUNT`, el comparador, los accesos rápidos y las notas al pie.

- [ ] **Step 3: Ejecutar, puerta, commit** — `feat(portada): la cinta invita a explorar las herramientas`.

---

### Task 4: Mazo de demostraciones (`#vault`)

**Files:**
- Create: `src/modules/landing/sections/vault-demos.tsx` (mover `FinanceDemo`, `CVOptimizerDemo`, `SecurityDemo`, `RestrictedOverlay`, `RestrictedBadge` y sus constantes desde `ForbiddenVaultSection.tsx:47-658`, exportándolos), `secciones/Mazo.tsx` (sustituye al stub), `secciones/mazo.css`, `datos/mazo.ts`
- Modify: `ForbiddenVaultSection.tsx` (importa desde `vault-demos.tsx`; se borra en la tarea 9)
- Test: `tests/e2e/portada-mazo.spec.ts`

**Interfaces:**
- `datos/mazo.ts`: `export const cartasMazo = [{ id: "finance", titulo: "Control Financiero", subtitulo: "Ingresos, gastos y planificación", color: "#5eead4", dorso: "Movimientos por categoría, presupuestos con alertas de desvío y planificación mensual desde un panel propio." }, { id: "cv", titulo: "Optimizador CV", subtitulo: "Revisión y estructura del documento", color: "#a78bfa", dorso: "Analiza la estructura del currículo, señala qué falta y propone un orden más claro para cada puesto." }, { id: "audit", titulo: "Auditoría de Seguridad", subtitulo: "Roles, sesiones y registro de actividad", color: "#f59e0b", dorso: "Registro de actividad por rol y sesión, con eventos revisables y alertas cuando algo se sale de lo esperado." }] as const;`
- `Mazo`: `section#vault.p-sec` con cabecera (eyebrow "Software de uso interno", `h2` "Infraestructura <span class="a">Privada</span>", párrafo actual de `ForbiddenVaultSection`), CTA "Conversar sobre un sistema" (`href="#contact"`), aviso "Vistas de demostración…" actual, y `.p-mazo` con tres `.p-carta-pos > div.p-carta[role="button"][tabindex=0][aria-pressed][data-demo=<id>][data-motion-active]`.

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("el mazo gira cada carta y congela sus demos fuera de pantalla", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await page.locator("#vault").scrollIntoViewIfNeeded();
  const cartas = page.locator("#vault [data-demo]");
  await expect(cartas).toHaveCount(3);
  await expect(page.locator('#vault [data-demo][data-motion-active="true"]').first()).toBeVisible();
  const primera = cartas.first();
  await expect(primera).toHaveAttribute("aria-pressed", "false");
  await primera.click();
  await expect(primera).toHaveAttribute("aria-pressed", "true");
  await expect(primera.locator(".p-dorso")).toBeVisible();
  await primera.press("Enter");
  await expect(primera).toHaveAttribute("aria-pressed", "false");
  await page.evaluate(() => document.getElementById("casos")!.scrollIntoView());
  await page.waitForTimeout(600);
  await expect(page.locator('#vault [data-demo][data-motion-active="true"]')).toHaveCount(0);
  await expect(page.locator("#vault")).not.toContainText(/200%|92%|850\+|Auditado/);
});
```

- [ ] **Step 2: Extraer las demos** a `vault-demos.tsx` con `export function FinanceDemo…`, `CVOptimizerDemo…`, `SecurityDemo…`; `ForbiddenVaultSection.tsx` las importa. `pnpm typecheck` verde.

- [ ] **Step 3: Mazo.tsx**

Portar `/* ───── mazo con los tres demos reales ───── */`: abanico (`rotate (i-1)*10`, `x (i-1)*sep()` con `sep = innerWidth < 640 ? 150 : 330`, `y |i-1|*22`), giro al pulsar (`.volteada` → `rotateY(180deg)`), `useInclinar` + `.p-brillo` en la cara, elevación al pasar. Cara: cabecera de la carta (icono, título, subtítulo) y la demo real (`FinanceDemo`, `CVOptimizerDemo`, `SecurityDemo` con `isActive = activa && nivel !== "estatico"`), dorso `.p-dorso` con el texto `dorso`. Actividad con `useMotionActivity()` por carta → `data-motion-active`. Enter/Espacio giran. Por debajo de 640 px las cartas se apilan en columna (sin duplicar DOM).

- [ ] **Step 4: Ejecutar, puerta, commit** — `feat(portada): mazo con las tres demostraciones reales`.

---

### Task 5: Coverflow de proyectos (`#casos`)

**Files:**
- Create: `secciones/Coverflow.tsx` (servidor), `secciones/CoverflowEscena.tsx` (cliente), `secciones/coverflow.css`, `datos/casos.ts`
- Modify: `src/app/page.tsx` (usa `Coverflow` en lugar de `ShowcaseSection`); `ShowcaseSection.tsx` se borra en la tarea 9
- Test: `tests/e2e/portada-casos.spec.ts`

**Interfaces:**
- `datos/casos.ts`: mover el array `projects` de `ShowcaseSection.tsx:8-51` tal cual (`title, category, description, details, evidence, href, cta, external, image, imageAlt, visualLabel`) más `fondo` (degradados del prototipo: `linear-gradient(135deg,#3f1d2e,#7c2d12)`, `linear-gradient(135deg,#0f2a3a,#134e4a)`, `linear-gradient(135deg,#1e1b4b,#0f172a)`). Sin iconos lucide en los datos (el icono se elige en el componente por índice).
- `Coverflow` (servidor) renderiza `section#casos[aria-labelledby=projects-title].p-sec` con cabecera y `CoverflowEscena` recibiendo `casos`; `CoverflowEscena` renderiza los tres `article.p-panel[data-caso][aria-current]` con `next/image` (`fill`, `sizes="(min-width: 1024px) 660px, 84vw"`), la etiqueta `visualLabel`, `h3`, párrafo, `Link` con el `cta`, y los puntos/flechas (`button` con `aria-label="Ver <title>"`).

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("el coverflow fija la sección y cambia de proyecto con scroll y con los puntos", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await expect(page.locator("#projects-title")).toHaveText("De la necesidad al producto");
  await page.evaluate(() => document.getElementById("casos")!.scrollIntoView());
  await page.waitForTimeout(500);
  await expect(page.locator("#casos article")).toHaveCount(3);
  await expect(page.locator('#casos article[aria-current="true"] h3')).toHaveText("FloresDyD");
  await page.getByRole("button", { name: "Ver Herramientas de uso diario" }).click();
  await expect(page.locator('#casos article[aria-current="true"] h3')).toHaveText("Herramientas de uso diario");
  await page.mouse.wheel(0, 900); await page.waitForTimeout(800);
  const activo = await page.locator('#casos article[aria-current="true"] h3').textContent();
  expect(activo).toBeTruthy();
  await expect(page.locator("#casos")).not.toContainText(/200%|92%|850\+|Auditado/);
  await expect(page.locator("#casos").getByRole("heading", { level: 3, name: "FloresDyD" })).toBeVisible();
});

test("sin JavaScript los tres proyectos se ven con su imagen", async ({ browser }) => {
  const ctx = await browser.newContext({ javaScriptEnabled: false });
  const page = await ctx.newPage();
  await page.goto("/");
  await expect(page.locator("#casos img")).toHaveCount(3);
  await expect(page.locator("#casos")).toContainText("Esquema del proyecto");
  await ctx.close();
});
```

- [ ] **Step 2: Implementar** portando `/* ───── coverflow con los tres casos reales ───── */`: `coverPos(p)` (`xPercent`, `scale`, `rotateY`, `z`, `opacity` por distancia), ScrollTrigger con `pin: ".p-cover-pin"`, `start: "top top"`, `end: "+=1200"`, `scrub: .4`, `onUpdate` → `coverPos(progress * 2)`, solo en `completo` y `medio`; altura `min(450px, 52vh)`. En `estatico` (y sin JS mediante CSS `.p-cover-escena` con `display: grid` cuando `html:not(.js)`… usar la clase `data-nivel="estatico"` del contenedor) los paneles se muestran en fila con `position: static`. `aria-current="true"` en el panel activo; los puntos llaman a `coverPos` con animación y desplazan el scroll dentro del pin (`gsap.to(window, { scrollTo })` no está disponible: usar `lenis?.scrollTo` o `window.scrollTo` a `st.start + fraccion * 1200`).

- [ ] **Step 3: Ejecutar, puerta, commit** — `feat(portada): coverflow de proyectos fijado al scroll`.

---

### Task 6: Órbita de tecnologías (`#tecnologias`)

**Files:**
- Create: `datos/tecnologias.ts` (mover el array `technologies` de `TechnologiesSection.tsx:7-70`, exportando `{ name, color, path? , isLucide? }`), `secciones/Orbita.tsx` (sustituye al stub), `secciones/orbita.css`
- Test: `tests/e2e/portada-orbita.spec.ts`

**Interfaces:** `Orbita` renderiza `section#tecnologias.p-sec` con cabecera centrada (`h2` "Una base <span class="m">para crecer</span>", párrafo actual), `.p-orbita-escena` con `#orbNombre[aria-live=polite]` (`<span class="ln"><b/></span><small>en el stack</small>`), el halo, y doce `button.p-satelite[aria-label=name]` con el SVG (`path` de Simple Icons o `Sparkles` de lucide para AI/ML). `<noscript>` con la rejilla actual de nombres.

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("la órbita revela el nombre de la tecnología en el centro", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await page.evaluate(() => document.getElementById("tecnologias")!.scrollIntoView());
  const sec = page.locator("#tecnologias");
  await expect(sec).not.toHaveAttribute("aria-busy", "true", { timeout: 10_000 });
  await expect(sec.getByRole("heading", { level: 2 })).toContainText("Una base");
  await expect(sec.locator("button.p-satelite")).toHaveCount(12);
  await expect(sec.locator("#orbNombre b")).toHaveText("");
  await sec.getByRole("button", { name: "PostgreSQL" }).hover({ force: true });
  await expect(sec.locator("#orbNombre b")).toHaveText("PostgreSQL");
  await sec.getByRole("button", { name: "Docker" }).click({ force: true });
  await expect(sec.locator("#orbNombre b")).toHaveText("Docker");
  expect(await sec.locator("#orbNombre b").evaluate((el) => getComputedStyle(el).color)).not.toBe("rgb(255, 255, 255)");
});
```

- [ ] **Step 2: Implementar** portando `/* ───── órbita de iconos (sin nombres) ───── */` y `/* ───── órbita: el nombre sube por el centro… ───── */`: anillo girando (`gsap.to(anillo, { rotate: 360, duration: 60, repeat: -1, ease: "none" })`, contrarrotación de cada `svg` para mantenerlos derechos), ralentización al activar (`timeScale(.25)`), entrada escalonada de los `svg` (no del `.p-satelite`, cuyo transform lo fija el CSS), nombre con `color: tech.color` y halo `--tono`. Solo anima si `useMotionActivity()` está activo. Radio `min(260px, 38vw)`.

- [ ] **Step 3: Ejecutar, puerta, commit** — `feat(portada): órbita de tecnologías con revelado del nombre`.

---

### Task 7: Centinela de seguridad (`#architecture`)

**Files:**
- Create: `datos/seguridad.ts`, `secciones/Centinela.tsx` (sustituye al stub), `secciones/centinela.css`, `secciones/matrixOrb.ts`, `docs/licencias/rareui-matrix-orb.md`
- Test: `tests/e2e/portada-centinela.spec.ts`

**Interfaces:**
- `datos/seguridad.ts`: `presets` del prototipo (`/* ───── seguridad: ponla a prueba ───── */`, `var presets = [...]`: `normal` hasta el anillo 4 → 200, `sql` hasta 2 → 400, `rafaga` hasta 2 → 429, `sesion` hasta 3 → 302 `/acceso`) con `{ id, nombre, tono, peticion: string[], hasta: number, codigo: number, veredicto, lineas: [clave, valor, estado][] }`; `cabecerasEsperadas = ["content-security-policy","strict-transport-security","x-frame-options","x-content-type-options","referrer-policy","permissions-policy","x-security-version"]`; `practicas` = las seis cadenas de `SecurityArchitectureSection.tsx:8-15`.
- `matrixOrb(canvas, { size, color, dots })` → `{ setState(s: "idle" | "block" | "ok"), setColor(c), start(), stop() }` (port literal del prototipo, crédito MIT en cabecera del archivo).
- `Centinela`: `section#architecture.p-sec` con cabecera, `.p-lanzador` (`h3` "Ponla a prueba", `role="group"` de cuatro `button.p-preset[aria-pressed]`, `.p-peticion[aria-live=polite]`, `button.p-cta` "Lanzar petición"), `.p-centinela` (rayo, dos SVG de anillos, núcleo con `canvas` y `span[role=status]` "Vigilando"/"Bloqueando"/"Correcto", veredicto), terminal `pre#term` y crédito.

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("el centinela bloquea una inyección y deja pasar una petición normal", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 1000 });
  await page.goto("/");
  await page.evaluate(() => document.getElementById("architecture")!.scrollIntoView());
  const sec = page.locator("#architecture");
  await expect(sec).not.toHaveAttribute("aria-busy", "true", { timeout: 10_000 });
  await expect(sec.getByRole("heading", { level: 2 })).toContainText("La seguridad no es un extra.");
  await sec.getByRole("button", { name: /Inyección SQL/ }).click();
  await sec.getByRole("button", { name: "Lanzar petición" }).click();
  await expect(sec.getByRole("status")).toHaveText("Bloqueando", { timeout: 8000 });
  await expect(sec.locator("#term")).toContainText("400", { timeout: 8000 });
  await sec.getByRole("button", { name: /Normal/ }).click();
  await sec.getByRole("button", { name: "Lanzar petición" }).click();
  await expect(sec.locator("#term")).toContainText("200", { timeout: 8000 });
  await expect(sec.locator("#term")).toContainText("content-security-policy");
  await expect(sec.getByText("Matrix Orb")).toBeVisible();
  await expect(sec).toContainText("CSP con nonces dinámicos");
});
```

- [ ] **Step 2: Implementar** portando el bloque completo (`trazar()` con `K.v` scrubbed por scroll 0.34→0.62, `colocarRayo`, `marcar`, `lanzarPeticion` con `clearTimeout(temporizador)`, `programar` del ciclo automático, `escribir(lineas)`). Las cabeceras reales: al montar, `fetch("/", { method: "HEAD" })` y leer `cabecerasEsperadas` de `res.headers` (valores acortados a 60 caracteres); si falla, usar las `lineas` del preset normal. El terminal escribe `curl -I https://nicoholas.dev` como primera línea. IntersectionObserver arranca/para el orb y el ciclo; `nivel === "estatico"` muestra todo en estado final sin bucles. Copiar la licencia MIT de rareui (`https://github.com/…/rareui` o el `LICENSE` del registro) en `docs/licencias/rareui-matrix-orb.md` con la URL de origen.

- [ ] **Step 3: Ejecutar, puerta, commit** — `feat(portada): centinela de seguridad con Matrix Orb y cabeceras reales`.

---

### Task 8: Contacto sin tarjetas

**Files:**
- Modify: `src/modules/landing/sections/ContactSection.tsx:224-254` (quitar el bloque de las dos tarjetas), CTA "Enviar Mensaje" con `data-magnetic` + `onda`
- Test: `tests/e2e/portada-contacto.spec.ts`

- [ ] **Step 1: Prueba**

```ts
import { test, expect } from "@playwright/test";
test("contacto sin las dos tarjetas y con el formulario intacto", async ({ page }) => {
  await page.goto("/");
  await page.evaluate(() => document.getElementById("contact")!.scrollIntoView());
  const sec = page.locator("#contact");
  await expect(sec).not.toHaveAttribute("aria-busy", "true", { timeout: 10_000 });
  await expect(sec).not.toContainText("Velocidad de ejecución");
  await expect(sec).not.toContainText("Calidad industrial");
  await expect(sec.getByRole("heading", { level: 2 })).toContainText("Hablemos de tu");
  await expect(sec.getByRole("button", { name: "Enviar Mensaje" })).toBeVisible();
  await expect(sec.getByText("Respuesta rápida")).toBeVisible();
});
```

- [ ] **Step 2: Implementar y commit** — `feat(portada): contacto sin tarjetas de servicio`.

---

### Task 9: Integración, contrato de pruebas, limpieza y rendimiento

**Files:**
- Modify: `tests/e2e/landing-motion.spec.ts` (reescribir al contrato de la especificación: fusionar las comprobaciones de `portada-*.spec.ts` que sean redundantes o mantenerlas en sus archivos y dejar aquí pausa/persistencia, firma, reducido/ahorro, sin JS, desbordamientos y foco de la barra)
- Delete: `HeroDashboard.tsx`, `FloatingDashboard.tsx`, `HeroContent.tsx`, `sections/HeroSection.tsx`, `ForbiddenVaultSection.tsx`, `SecurityArchitectureSection.tsx`, `TechnologiesSection.tsx`, `ShowcaseSection.tsx`, `BackgroundManager.tsx` (comprobar con `grep -rn` que nadie los importa; `recharts` deja de usarse en la portada — no se desinstala en esta tarea)
- Docs: `docs/2026-09-15-portada-viva.md` (entrega: qué cambió, cómo se verificó, cifras)

- [ ] **Step 1:** Reescribir `landing-motion.spec.ts` según "Invariantes de pruebas" de la especificación (puntos 2, 8, 9, 10 y 11; los demás viven en `portada-*.spec.ts`).
- [ ] **Step 2:** Suite completa: `REDIS_URL='' npx playwright test --workers=1 --reporter=line` (las 9 de `autonomous-defense` necesitan Redis y se ignoran). Todo lo demás verde.
- [ ] **Step 3:** Rendimiento: `pnpm build && PORT=3100 pnpm start`; Lighthouse 12 móvil (3 corridas, mediana) y escritorio contra `http://localhost:3100/` (con `CHROME_PATH=~/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome`); perfil de frames en Pixel 7 emulado con CPU ×4 (script `perfil-movil.js` del taller adaptado). Objetivos: móvil ≥ 90, LCP ≤ 2,5 s, CLS < 0,1, accesibilidad 100; escritorio ≥ 95; frame medio ≤ 50 ms. Corregir lo que falle (densidad del fondo, resolución, demos) hasta cumplir.
- [ ] **Step 4:** Capturas a 1440 y 390 de cada sección (una pasada) y revisión visual contra el prototipo.
- [ ] **Step 5:** Puerta y commit `feat(portada): integración, contrato de pruebas y limpieza de secciones retiradas`; documento de entrega.

---

### Task 10: Despliegue cuidadoso al VPS compartido

Sin tocar nginx, compose, secretos ni bases de datos. Solo se recrea `portfolio_web`.

- [ ] **Step 1:** Anotar el commit final (`git rev-parse HEAD`) y hacer `git push -u origin ui/portada`.
- [ ] **Step 2:** Worktree limpio: `git -C <worktree> checkout <commit>`; `corepack pnpm@10.33.0 install --frozen-lockfile`; `DATABASE_URL="postgresql://user:pass@localhost:5432/db" NEXT_TELEMETRY_DISABLED=1 corepack pnpm@10.33.0 run build`; comprobar `.next/standalone/server.js`, `.next/static`, `public` y `.next/BUILD_ID`.
- [ ] **Step 3:** Empaquetar: `tar -czf portada-build.tar.gz .next/standalone .next/static public` (desde `APP/NEXT_APP` del worktree). Subir: `scp portada-build.tar.gz teguihd@13.88.244.94:/tmp/`.
- [ ] **Step 4:** En el VPS (una sesión, `set -e`): `free -m`; `docker tag portfolio_web:latest portfolio_web:rollback-$(date +%Y%m%d-%H%M)`; `cd ~/portfolio && rm -rf .next/standalone .next/static && tar -xzf /tmp/portada-build.tar.gz && rm /tmp/portada-build.tar.gz`; `docker build -f Dockerfile.prod -t portfolio_web:latest .`; `cd DOCKER && docker compose --env-file .env up -d --no-deps web`; `sleep 8; docker ps --format '{{.Names}}\t{{.Status}}' | grep portfolio; docker logs portfolio_web --tail 20; curl -sI http://127.0.0.1:3000/ | head -3`.
- [ ] **Step 5:** Desde fuera: `curl -sI https://nicoholas.dev/ | grep -iE "HTTP|content-security|x-security"`; prueba de humo Playwright contra `https://nicoholas.dev` (portada con "Usar herramientas", `/herramientas` responde, `/herramientas/quitar-fondo` con carril de la mesa, sin errores JS en consola); Lighthouse móvil y escritorio contra producción; comprobar que `sicove.cl` y `asistencia.nicoholas.dev` siguen respondiendo 200.
- [ ] **Step 6:** Si algo falla: `docker tag portfolio_web:rollback-<fecha> portfolio_web:latest && cd ~/portfolio/DOCKER && docker compose --env-file .env up -d --no-deps web`.
- [ ] **Step 7:** Registrar en `docs/2026-09-15-portada-viva.md` el commit desplegado, la etiqueta de rollback y las cifras de producción.
